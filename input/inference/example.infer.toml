# Config file for inference scheme

# Set seed for reproducibility
seed = 42

# Logging level ("DEBUG", "INFO", "WARNING", "ERROR")
logging = "INFO"

# Path to output folder where inference will be saved (relative to PROTEUS output folder)
output = "bayesopt_infer_se/"

# Path to base (reference) config file relative to PROTEUS root folder
ref_config = "output/bayesopt_se/init_coupler.toml"

# Method for initialising the inference scheme (one of these must be 'none')
init_samps = 3        # Number of random samples if starting from scratch.
init_grid  = 'none' # grid_demo/'   # Path pre-computed grid (relative to PROTEUS output folder)

# Excluded exit codes (in addition to errors)
failure_codes = [10, 15] # solidified, escaped

# Parameters for Bayesian optimisation
n_workers  = 10        # Number of parallel workers
kernel     = "MAT3/2"    # Kernel type for GP, "RBF" | "MAT1/2" | "MAT3/2" | "MAT5/2"
acqf       = "LogEI"  # Acquisition function, "UCB" | "LogEI" | "E-LogEI"
n_steps    = 60       # Total number of evaluations (i.e. BO steps)

# Parameters to optimize (with bounds)
[parameters]
# "params.stop.time.maximum"  = [1e7,     3e9] # for SN only
# "orbit.semimajoraxis"       = [0.05,    0.2] # for SN only
"interior_energetics.boundary.T_p_0"   = [2000.0,  4000.0]
# "planet.mass_tot"           = [0.8,     4.0]
"interior_struct.core_frac"           = [0.2,     0.8]
"outgas.fO2_shift_IW"       = [-3.5,    5.0]
"delivery.elements.H_budget"= [1.0,     750.0]      # oceans
"delivery.elements.C_budget"= [0.1,     6.0]        # C/H
"delivery.elements.S_budget"= [0.1,     6.0]        # S/H


# Target observables to match by optimisation...

# helpfile: /data/hen28/Projects/PROTEUS/output/bayesopt_sn/runtime_helpfile.csv
# extracted at index: 22, time: 6.000e+08 years
# [observables]
# "M_planet" = 1.8297008682e+25
# "R_obs" = 1.1747474378e+07
# "T_obs" = 3.8535958653e+02
# "atm_kg_per_mol" = 5.0096499771e-03
# "C/O_atm" = 3.7871308185e+00
# "S/O_atm" = 1.0580664287e+00

# helpfile: /data/hen28/Projects/PROTEUS/output/bayesopt_se/runtime_helpfile.csv
# extracted at index: 24, time: 1.000e+09 years
[observables]
"M_planet" = 1.2862326572e+25
"R_obs" = 7.5436421662e+06
"T_obs" = 5.0064853688e+02
"atm_kg_per_mol" = 2.7450258753e-02
"C/O_atm" = 3.4886348812e-01
"S/O_atm" = 6.5449797851e-02

# helpfile: /data/hen28/Projects/PROTEUS/output/bayesopt_sn/runtime_helpfile.csv
# extracted at index: 34, time: 6.000e+08 years
# [observables]
# "M_planet" = 2.1751937195e+25
# "R_obs" = 1.0205859280e+07
# "T_obs" = 3.6520334845e+02
# "atm_kg_per_mol" = 1.1326178337e-02
# "C/O_atm" = 1.0219849141e+00
# "S/O_atm" = 1.2773384323e+00
